<div class="sidebar-wrapper">

				<div class="subhead" id="artist-name">
					<%= @artist.amg_artist_name %>
				</div>
			
				<%= render partial: 'image' %>
				<br>
				<%= render partial: 'sidebar' %>

</div>


<div id="tree-content">

		<div class="sky">

			<h1 class="headline">Bandly Tree</h1>
			<h2 class="subhead">Know Your Roots</h2>
			
			<img class="tree" src="http://2.bp.blogspot.com/_VC5iSShRPRs/S8XvYuq4-SI/AAAAAAAABJc/vm418GSuQIA/s1600/Ficusmcp04L.png">

			<div class="search-bar">
				<%= form_for @query, remote: true do |f| %>
					<%= f.label "search" %>
					<%= f.text_field :artist_name, :id => "textBar" %>
					<%= f.submit %>
				<% end %>
			</div>
		
		</div>


		<div class="ground">
			
	<!-- display "No Results" if page object parse array comes up empty/nil -->

			<div id="influencers-1">
				<% if @artist.related[0] == nil %>

					<div class="influencers">
						No Results
					</div>
				<% else %>
	<!-- display up to four influencers -->
					<% @artist.related.each do |i| %>
						<div class="influencers">
							<%= i.text %>
							<br>
							<%= form_for @query, :html => {:id => "banana"}, remote: true do |f| %>
								<%= f.hidden_field :artist_name, :value => i.text %>
								<%= f.submit "See More" %>
							<% end %>
							<button type="button" class="btn btn-success see-more">VVV</button>
						</div>
					<% end %>
				<% end %>
			</div>

		</div>

</div>



<!-- work in progress - do not remove -->
<!-- need to add query form -->

<script>
	
	$(document).ready(function(){

	$('.see-more').click(function(){
		alert("woot");
		$("#influencers-1").append("<div id='influencers-2'><% if @artist.root_row[0] == nil %><div class='influencers'>No Results</div><% else %><% @artist.root_row.each do |i| %><div class='influencers'><%= i.artist_name %><br><button type='button' class='btn btn-success see-more2'>VVV</button></div><% end %><% end %></div>");

		// <div class="ground">
		// 	<div id="influencers-1">
		// 		<% if @artist.related[0] == nil %>
		// 			<div class="influencers">
		// 				No Results
		// 			</div>
		// 		<% else %>
		// 			<% @artist.related.each do |i| %>
		// 				<div class="influencers">
		// 					<%= i.text %>
		// 					<br>
		// 					<%= form_for @query, :html => {:id => "banana"}, remote: true do |f| %>
		// 						<%= f.hidden_field :artist_name, :value => i.text %>
		// 						<%= f.submit "See More" %>
		// 					<% end %>
		// 					<button type="button" class="btn btn-success see-more">VVV</button>
		// 				</div>
		// 			<% end %>
		// 		<% end %>
		// 	</div>
		// </div>


		// window.scrollTo(0,document.body.scrollHeight);
	})

});

</script>